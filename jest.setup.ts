// jest-expo handles most native module mocks automatically.
//
// Add project-specific mocks here as Phase 2 tests grow. Examples that will
// likely be needed (commented out until a test actually needs them):
//
//   // Reanimated v4 ships a JS-only mock for tests
//   jest.mock('react-native-reanimated', () => require('react-native-reanimated/mock'));
//
//   // expo-secure-store — stub for token storage tests
//   jest.mock('expo-secure-store', () => ({
//     getItemAsync: jest.fn(),
//     setItemAsync: jest.fn(),
//     deleteItemAsync: jest.fn(),
//   }));
//
//   // expo-auth-session — stub for Auth0 PKCE flow tests
//   jest.mock('expo-auth-session', () => ({ ... }));

export {};
